# Please refer https://aka.ms/HTTPSinContainer on how to setup an https developer certificate for your ASP .NET Core service.

version: '3.4'

services:
  provider:
    image: provider
    build:
      context: .
      dockerfile: src/provider/Dockerfile
      network: "host"
    ports:
      - 5000:5000
    volumes:
      - ./.providerVolume:/app/volume
